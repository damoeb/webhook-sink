{
  "name": "webhook-sink",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.15.2",
    "lodash": "^4.17.4",
    "mongodb": "^2.2.24",
    "promise": "^7.1.1"
  },
  "scripts": {
    "serve": "mkdir .tmp && mkdir .tmp/mongo && concurrently --kill-others \"node app/server\" \"npm run mongo\"",
    "mongo": "mongod --dbpath .tmp/mongo"
  },
  "devDependencies": {
    "concurrently": "^3.4.0"
  }
}
